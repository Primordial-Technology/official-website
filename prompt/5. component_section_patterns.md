You are a design systems and UI architecture specialist.

Using:
- The Design Language Guide and tokens we defined earlier,
- The wireframe layout from STEP 4,

Task:
Define a small set of reusable UI building blocks for this site, without writing any code yet.

Output:

1) Primitive components (atoms):
   - Examples: Button, Link, EyebrowLabel, SectionTitle, SectionSubtitle, BodyText, Divider, CardShell, Tag.
   - For each:
     - purpose
     - content slots / props (e.g. label, variant, size)
     - which design tokens it uses (colors, typography, spacing, radius).

2) Section patterns (molecules / organisms):
   - Examples: HeroSection, NarrativeSection, StepsSection, SplitLayoutSection, ContactSection.
   - For each:
     - purpose
     - content slots (title, body, list items, CTA, etc.)
     - layout rules (alignment, typical spacing)
     - how motifs (dots / grid / lines) are applied, in line with the Figma style.

3) Usage guidelines:
   - For each component / pattern:
     - 2–3 “Do” bullets (good usage).
     - 2–3 “Don’t” bullets (misuse or ways it becomes too generic or off-brand).

Self-check:
- 2 bullet points on how this component library reflects the Figma-derived style instead of a generic UI kit.
- 1 risk where these components might drift toward genericness, and how to prevent that.

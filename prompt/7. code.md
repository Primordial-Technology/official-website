You are a senior front-end engineer and UI developer.

Using:
- The Design Language Guide and CSS tokens we defined,
- The chosen color palette and hero visual concept,
- The information architecture and wireframe layout,
- The component and section patterns,
- And the full-page visual spec,

Task:
Implement the entire one-page website for Primordial as a single React component.

Technical constraints:
- Use semantic HTML tags: <header>, <main>, <section>, <footer>, <nav>, etc.
- Center a narrow content column (~720px) with generous vertical padding.
- Use [Tailwind CSS utility classes OR CSS modules / styled-components — choose one and stick to it].
- Define design tokens in one place:
  - Either as CSS custom properties in a `:root` block,
  - Or as Tailwind config-style utility classes.
  - Avoid scattering raw hex codes and magic spacing numbers throughout the JSX.

Visual implementation:
- Implement the hero background:
  - Either as a gradient based on our palette, or by referencing a hero image URL.
  - If using CSS-only abstraction, use `background-image` with gradients and pseudo-elements.
- Implement the recurring motifs (dots / grids / lines / frames):
  - Use CSS (background-image, linear-gradient, radial-gradient, pseudo-elements).
  - Keep them low-contrast and behind the text.
  - Reuse them in at least 2–3 sections, as defined in the visual spec.

Interactions:
- Implement smooth scrolling for in-page navigation (e.g. clicking header links scrolls to sections).
- Add hover and focus states for:
  - primary and secondary buttons
  - text links.
- Keep transitions subtle (e.g. opacity, border-color, background-color); avoid heavy animations.

Component structure:
- Implement primitives (buttons, section titles, etc.) as small React components where it makes sense.
- Implement section components (HeroSection, NarrativeSection, StepsSection, ContactSection) according to the patterns from STEP 5.
- Compose the final page from these sections inside a single top-level component.

Comments:
- Add brief comments in the code where you:
  - apply the hero visual concept,
  - use key accent colors,
  - implement motif patterns,
  - make non-obvious layout decisions.
- Comments should explain design intent, not trivial HTML.

Output:
- A complete React component (plus CSS / Tailwind code) in a single code block.
- Ensure the code is syntactically valid and can be pasted into a project.

Self-check (after the code):
- 3 bullet points on how the implementation reflects the Figma-derived design language and stays within the tokens.
- 2 bullet points on how it avoids generic SaaS layout patterns and visuals.
